# Isolated WebGL benchmark environment
Temporary branch for public-library installation and public-reference browser capture. No existing application code, credentials or private files are used or deployed. Main is unchanged.
